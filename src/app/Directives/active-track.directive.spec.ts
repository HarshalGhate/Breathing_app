import { ActiveTrackDirective } from './active-track.directive';

describe('ActiveTrackDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveTrackDirective();
    expect(directive).toBeTruthy();
  });
});
